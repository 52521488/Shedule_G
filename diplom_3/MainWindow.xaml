<Window x:Class="diplom_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:diplom_3.Core"
        xmlns:conv="clr-namespace:diplom_3.Converters"
        mc:Ignorable="d"
        Title="Электронное расписание колледжа" 
        Height="650" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <conv:ScaleConverter x:Key="ScaleConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:LessonToBrushConverter x:Key="LessonBrushConv"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Меню / тулбар -->
            <RowDefinition Height="*"/>
            <!-- Основная область -->
        </Grid.RowDefinitions>

        <!-- Тулбар сверху -->
        <ToolBar Grid.Row="0" Background="#F5F5F5">
            <Button Content="Добавить занятие" Click="AddLesson_Click" Margin="5"/>
            <Button Content="Обновить" Click="Refresh_Click" Margin="5"/>
            <Separator/>
            <TextBlock Text="Масштаб:" VerticalAlignment="Center" Margin="10,0"/>
            <Slider x:Name="zoomSlider" 
                    Minimum="0.6" Maximum="2.2" Value="1.0" 
                    Width="180" Margin="5,0" 
                    ValueChanged="zoomSlider_ValueChanged"/>
            <TextBlock Text="{Binding Value, ElementName=zoomSlider, StringFormat={}{0:P0}}" 
                       VerticalAlignment="Center"/>
        </ToolBar>

        <!-- Основная область с расписанием -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="scheduleContainer" 
                  Margin="10"
                  LayoutTransform="{Binding ElementName=zoomSlider, Path=Value, Converter={StaticResource ScaleConverter}}">
                <!-- Здесь динамически строится таблица в коде -->
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>